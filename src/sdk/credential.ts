// Public SDK surface for thomas credential shapes.
//
// Adapter `extractCredentials()` returns these. Re-exported from the canonical
// location in src/config/credentials.ts so out-of-tree adapters have a stable
// import path.

export type { Credential, SecretRef } from "../config/credentials.js";
