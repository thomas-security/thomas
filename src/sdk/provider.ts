// Public SDK surface for thomas provider specs.
//
// `ExtractedCredential.provider` carries this; out-of-tree adapters that want
// to register their endpoint alongside a credential need this type.

export type { ProviderSpec } from "../providers/registry.js";
